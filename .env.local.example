# Required — generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
SESSION_SECRET=replace_with_at_least_32_char_random_string

# Optional — override default data file paths
# REVOPS_DASHBOARD_DB_PATH=/custom/path/revops-dashboard.db
# REVOPS_LEGACY_SNAPSHOT_PATH=/custom/path/weekly-metrics.json
# REVOPS_USERS_FILE_PATH=/custom/path/users.json
